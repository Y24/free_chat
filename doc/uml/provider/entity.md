# entity

``` mermaid
classDiagram

 class ProviderEntity{
     +ProviderEntity emptyProviderEntity
     +fromJson(json)
     +json toJson()
     +get hashCode
     +toString()
     +operator ==
     ...
     ...()
 }
```
